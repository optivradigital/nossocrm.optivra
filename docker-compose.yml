version: "3.9"
services:
  nossocrm:
    build: .
    container_name: nossocrm
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
